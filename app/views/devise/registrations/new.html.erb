<!DOCTYPE html>
<html lang="en">
<head>
  <title>Member Sign Up</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</head>

<body>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>
    <h2>Sign Up Here!</h2>

    <div class="field signup-email-field">
      <%= f.label :Email_Address, class: "sr-only" %>
      <%= f.email_field :email, autofocus: true, placeholder: "Email Address..." %>
    </div> <!-- end field -->

    <div class="field signup-password-row">
      <%= f.label :Create_Your_Password, class: "sr-only" %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "off", placeholder: "Password..." %>
    </div> <!-- end field -->

    <div class="field signup-password2-field">
      <%= f.label :Confirm_Password, class: "sr-only" %>
      <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Confirm Password..." %>
    </div> <!-- end field -->
    
    <fieldset class="form-group">
      <legend class="sr-only">Radio button</legend>
      <div class="form-check">
        <label class="form-check-label">
          <input type="radio" class="form-check-input" name="remember-password-radio" id="password-radio-new" value="option1">Remember Me
        </label>
      </div> <!-- end form-check -->
    </fieldset>

    </fieldset>

    <div class="actions">
    <%= f.submit "Sign up" %>
    </div> <!-- end actions -->

  <% end %>

<%= render "devise/shared/links" %>
